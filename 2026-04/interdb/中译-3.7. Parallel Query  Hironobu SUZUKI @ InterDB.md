---
---

# 3.7. Parallel Query

`Parallel Query` 是从 9.6 版本开始支持的特性，它使用多个进程（Worker）来处理单个查询。

例如，在满足某些条件时，执行查询的 PostgreSQL 进程会成为 Leader，并启动若干 Worker 进程（最大数量由 `max_parallel_workers_per_gather` 决定）。然后每个 Worker 进程执行扫描处理，并将结果依次返回给 Leader 进程。Leader 进程再聚合这些由 Worker 进程返回的结果。

图 3.38 展示了由两个 Worker 进程处理顺序扫描查询的过程。

![](https://www.interdb.jp/pg/pgsql03/fig-3-38.png)

图 3.38. Parallel Query 概念图。

在 14 版本引入的配置参数 `parallel_leader_participation` 允许 Leader 进程在等待 Worker 响应时也参与处理查询。默认值为 `on`。

不过为了简化说明与图示，我们将在假定 Leader 进程不处理查询（即 parallel\_leader\_participation 为 `off`）的前提下展开解释。

Parallel Query 在逐步完善之中。表 3.2 摘录了官方文档 release notes 中关于 Parallel Query 的若干要点。

| 版本 | 描述 |
| --- | --- |
| 9.6 | - Seq Scan。- Nested Loop Join 与 Hash Join。 |
| 10 | - Merge Join。- B-tree Index Scan、Bitmap Heap Scan。- 允许非相关子查询。 |
| 11 | - CREATE INDEX 在构建 B-tree 索引时可以使用并行处理。- 允许 hash join 通过共享 hash table 并行执行。- 允许 UNION 在各自 SELECT 无法单独并行时，将每个 SELECT 并行执行。- 允许并行化 CREATE TABLE... AS、SELECT INTO 和 CREATE MATERIALIZED VIEW 等命令。 |
| 12 | - 允许在 SERIALIZABLE 隔离级别下并行化查询。 |
| 14 | - 允许引用多个 foreign table 的查询并行执行 foreign table scan。- 允许 REFRESH MATERIALIZED VIEW 使用并行。 |
| 15 | - 允许 SELECT DISTINCT 并行化。- 在更多场景下允许引用多个 foreign table 的查询并行执行 foreign table scan。- 允许在 postgres\_fdw 服务器上并行提交。 |
| 16 | - 允许 FULL 以及内部 right OUTER hash join 并行化。- 允许 postgres\_fdw 并行执行 abort。 |

需要注意的是，Parallel Query 主要面向 READ-ONLY 场景，目前尚不支持游标操作。

接下来，我们会先概述 Parallel Query，再探讨 Join 操作，最后说明聚合函数在并行查询中如何计算。

本节目录

- `3.7.1. Overview`
- `3.7.2. Parallel Join`
- `3.7.3. Parallel Aggregate`

## 3.7.1. Overview

图 3.39 简要描述了 PostgreSQL 中并行查询的执行过程。

![](https://www.interdb.jp/pg/pgsql03/fig-3-39.png)

图 3.39. Parallel Query 的执行过程。

1. Leader 创建 Plan：
	优化器创建一个可以并行执行的 plan。
2. Leader 存储共享信息：
	为了让 Leader 节点和 Worker 节点都能执行该 plan，Leader 会将必要的信息存放到自己的 Dynamic Shared Memory (DSM) 区域中。更多细节见下文说明。
3. Leader 创建 Worker。
4. Worker 设置状态：
	每个 Worker 读取存储的共享信息来设置自己的状态，从而与 Leader 保持一致的执行环境。
5. Worker 扫描行并返回结果。
6. Leader 汇总结果。
7. 查询完成后，Worker 被终止，Leader 释放 DSM 区域。

在 Parallel Query 处理过程中，Leader 和 Worker 进程通过 Dynamic Shared Memory (DSM) 区域进行通信。

Leader 进程按需分配内存空间（即可以在需要时分配，不再需要时释放）。Worker 进程随后可以像读写自己的内存一样，对这些内存区域进行读写。

本节中，为了用具体示例进行演示，我们使用如下的表 `d`：

```sql
testdb=# CREATE TABLE d (id double precision, data int);
CREATE TABLE
testdb=# INSERT INTO d SELECT i::double precision, (random()*1000)::int FROM generate_series(1, 1000000) AS i;
INSERT 0 1000000
testdb=# ANALYZE;
ANALYZE
```

### 3.7.1.1. Creating Parallel Plan

Parallel Query 并不总是会被考虑。如果待扫描的表大小大于等于 `min_parallel_table_scan_size` （默认值为 8MB），或者待扫描的索引大小大于等于 `min_parallel_index_scan_size` （默认值为 512kB），优化器就会考虑使用 Parallel Query。

下面是一个最简单的并行查询 plan：

```sql
testdb=# EXPLAIN SELECT * FROM d WHERE id BETWEEN 1 AND 100;
                                     QUERY PLAN
-------------------------------------------------------------------------------------
 Gather  (cost=1000.00..16609.10 rows=1 width=12)
   Workers Planned: 2
   ->  Parallel Seq Scan on d  (cost=0.00..15609.00 rows=1 width=12)
         Filter: ((id >= '1'::double precision) AND (id <= '100'::double precision))
rows
```

如上所示，最简单的 plan 由一个 `Gather` 节点和一个 `Seq Scan` 节点构成。

图 3.40 展示了上述查询的 plan tree：

![](https://www.interdb.jp/pg/pgsql03/fig-3-40.png)

图 3.40. Leader 的 Plan Tree

Gather 节点是 Parallel Query 专用的节点，负责收集来自 Worker 的结果。除了 Gather 之外，Parallel Query 还有以下专用节点：

- GatherMerge
- Append 和 AppendMerge（见
	`Official Document`。）
- Finalize/Partial Aggregate（见
	`Section 3.7.3`。）

由 Worker 执行的节点通常是 Gather 节点之下的 subplan tree，并且其 `parallel_safe` 值被设置为 `True`。在上述示例中，位于 Gather 节点之下的 Seq Scan 节点及其下方节点，都是由 Worker 执行的。

### 3.7.1.2. Storing Shared Information

为了与 Worker 协同执行查询，Leader 会将需要与 Worker 共享的信息存放在自己的 DSM (Dynamic Shared Memory) 区域中。

Leader 与 Worker 共享两类信息：execution state 和 query。

- Execution State：
	Leader 和 Worker 一致地执行同一查询所需的环境信息。详情见 `README.parallel`。包括以下信息：
	- 所有配置参数（GUC）
		- 事务快照、当前子事务的 XID。（详情见
		`Chapter 5`。）
		- 由 dfmgr.c 动态加载的库集合。
	这些信息由 `InitializeParallelDSM()` 存储。
- Query：
	包含以下信息：
	- PlannedStmt
		- ParamListInfo
		- Worker 执行的节点的 Description 结构。例如 SeqScan 节点使用 `ParallelTableScanDesc`；IndexScan 节点使用 `ParallelIndexScanDesc`。详情见
		`ExecParallelInitializeDSM()`。
		- 用于汇报目的的 instrumentation 和 usage 信息。
	这些信息由 `ExecInitParallelPlan()` 存储。

此外，Leader 会在 DSM 上创建一个 TupleQueue（基于 `tqueue.c` ），用于读取 Worker 的结果。

### 3.7.1.3. Creating Workers

查询 plan 中计划的 Worker 数量，可能与实际运行的 Worker 数量不同。这是因为 Worker 的最大数量受 `max_parallel_workers` 限制，并且由于其他并行查询进程的存在，可用 Worker 可能不够。

EXPLAIN ANALYZE 命令会同时显示 planned 与 launched 的 Worker 数量。

```sql
testdb=# EXPLAIN ANALYZE SELECT * FROM d WHERE id BETWEEN 1 AND 100;
                                                    QUERY PLAN
------------------------------------------------------------------------------------------------------------------
 Gather  (cost=1000.00..16609.10 rows=1 width=12) (actual time=60.035..60.994 rows=100 loops=1)
   Workers Planned: 2
   Workers Launched: 2
   ->  Parallel Seq Scan on d  (cost=0.00..15609.00 rows=1 width=12) (actual time=31.073..52.248 rows=50 loops=2)
         Filter: ((id >= '1'::double precision) AND (id <= '100'::double precision))
         Rows Removed by Filter: 499950
265
012
rows
```

### 3.7.1.4. Setting Up Workers

启动之后，Worker 会读取由 Leader 准备好的共享 Execution State 和 Query 信息。

通过设置 Execution State 信息，Worker 就可以在与 Leader 相同的环境中执行查询。

在读取 Query 信息时， `ExecSerializePlan()` 函数被用来基于 PlannedStmt 为 Worker 生成并保存一棵 plan tree。具体来说，会生成一棵 parallel\_safe 为 True 的新子 plan tree（通常是 Gather 节点的 subplan tree）作为 Worker 的 plan tree。

图 3.41 展示了 Worker 的 plan tree。

![](https://www.interdb.jp/pg/pgsql03/fig-3-41.png)

图 3.41. 从 Leader Plan Tree 生成的 Worker Plan Tree

### 3.7.1.5. Scanning Rows and Returning Results

如 `Section 3.4.1.1` 中所讨论，Executor 访问单条数据元组的方法高度抽象化。这一原则同样适用于 Parallel Query。

虽然这里略去了详细说明，但由于 Leader 和 Worker 通过 DSM 共享查询执行环境，单次顺序扫描可以由 Leader 和 Worker 各自调用 SeqNext() 函数来并行执行。

类似地，结果会通过 DSM 上的 TupleQueue 返回给 Gather 节点。

### 3.7.1.6. Gathering Results

Gather 节点作为 Parallel Query 专用节点，负责汇集 Worker 返回的结果。

## 3.7.2. Parallel Join

PostgreSQL 中的 Parallel Query 支持 nested-loop join、merge join 和 hash join。

本节中，我们将使用如下两个表：`d` 和 `f`：

```sql
testdb=# CREATE TABLE d (id double precision, data int);
CREATE TABLE
testdb=# INSERT INTO d SELECT i::double precision, (random()*1000)::int FROM generate_series(1, 1000000) AS i;
INSERT 0 1000000
testdb=# CREATE INDEX d_id_idx ON d (id);
CREATE INDEX
testdb=# CREATE TABLE f (id double precision, data int);
CREATE TABLE
testdb=# INSERT INTO f SELECT i::double precision, (random()*1000)::int FROM generate_series(1, 10000000) AS i;
INSERT 0 10000000
testdb=# \d d
                      Table "public.d"
 Column |       Type       | Collation | Nullable | Default
--------+------------------+-----------+----------+---------
 id     | double precision |           |          |
 data   | integer          |           |          |
Indexes:
    "d_id_idx" btree (id)

testdb=# \d f
                      Table "public.f"
 Column |       Type       | Collation | Nullable | Default
--------+------------------+-----------+----------+---------
 id     | double precision |           |          |
 data   | integer          |           |          |

testdb=# ANALYZE;
ANALYZE
```

### 3.7.2.1. Nested Loop Join

在 nested loop join 中，内表（inner table）总是需要处理所有行，并且不是并行处理的。

因此，举例来说，在 materialized nested loop join 中，每个 Worker 都必须独立地物化内表，效率较低。

```sql
testdb=# SET enable_nestloop = ON;
SET
testdb=# SET enable_mergejoin = OFF;
SET
testdb=# SET enable_hashjoin = OFF;
SET

testdb=# EXPLAIN SELECT * FROM d, f WHERE d.data = f.data AND f.id < 10000;
                                  QUERY PLAN
-------------------------------------------------------------------------------
 Gather  (cost=1000.00..97163469.29 rows=9651513 width=24)
   Workers Planned: 2
   ->  Nested Loop  (cost=0.00..96197317.99 rows=4825756 width=24)
         Join Filter: (d.data = f.data)
         ->  Parallel Seq Scan on f  (cost=0.00..121935.99 rows=4831 width=12)
               Filter: (id < '10000'::double precision)
         ->  Materialize  (cost=0.00..27992.00 rows=1000000 width=12)
               ->  Seq Scan on d  (cost=0.00..18109.00 rows=1000000 width=12)
rows
```
![](https://www.interdb.jp/pg/pgsql03/fig-3-42.png)

图 3.42. Parallel Query 中的 Materialize Nested Loop Join。

另一方面，在 Indexed Nested Loop Join 中，内表可以由 Worker 并行扫描，因此效率更高。

```sql
testdb=# EXPLAIN SELECT * FROM d, f WHERE d.id = f.id AND f.id < 10000;
                                  QUERY PLAN
-------------------------------------------------------------------------------
 Gather  (cost=1000.42..142818.71 rows=967 width=24)
   Workers Planned: 2
   ->  Nested Loop  (cost=0.42..141722.01 rows=484 width=24)
         ->  Parallel Seq Scan on f  (cost=0.00..121935.99 rows=4831 width=12)
               Filter: (id < '10000'::double precision)
         ->  Index Scan using d_id_idx on d  (cost=0.42..4.09 rows=1 width=12)
               Index Cond: (id = f.id)
rows
```
![](https://www.interdb.jp/pg/pgsql03/fig-3-43.png)

图 3.43. Parallel Query 中的 Indexed Nested Loop Join。

### 3.7.2.2. Merge Join

与 nested loop join 类似，merge join 也总是对内表的所有行进行处理，不会并行。因此每个 Worker 都会独立执行内表的排序过程。

但是，如果内表通过 index scan 访问，则可以像 Indexed Nested Loop Join 那样高效地完成 join 操作。

```sql
testdb=# SET enable_nestloop = OFF;
SET
testdb=# SET enable_mergejoin = ON;
SET
testdb=# SET enable_hashjoin = OFF;
SET
testdb=# EXPLAIN SELECT * FROM d, f WHERE d.id = f.id AND d.id < 100000;
                                       QUERY PLAN
----------------------------------------------------------------------------------------
 Gather  (cost=837387.83..853944.33 rows=97361 width=24)
   Workers Planned: 2
   ->  Merge Join  (cost=836387.83..843208.23 rows=48680 width=24)
         Merge Cond: (f.id = d.id)
         ->  Sort  (cost=836385.61..848880.80 rows=4998079 width=12)
               Sort Key: f.id
               ->  Parallel Seq Scan on f  (cost=0.00..109440.79 rows=4998079 width=12)
         ->  Index Scan using d_id_idx on d  (cost=0.42..3569.24 rows=97361 width=12)
               Index Cond: (id < '100000'::double precision)
rows
```

### 3.7.2.3. Hash Join

在 10 和 9.6 版本中，在 Parallel Query Hash Join 期间，每个 Worker 都会独立地为内表执行 hash table build 过程。

```sql
testdb=# SET enable_nestloop = OFF;
SET
testdb=# SET enable_mergejoin = OFF;
SET
testdb=# SET enable_hashjoin = ON;
SET
testdb=# SET enable_parallel_hash = OFF;
SET
testdb=# EXPLAIN SELECT * FROM d, f WHERE d.id = f.id;
                                    QUERY PLAN
----------------------------------------------------------------------------------
 Gather  (cost=36492.00..323368.59 rows=1000000 width=24)
   Workers Planned: 2
   ->  Hash Join  (cost=35492.00..222368.59 rows=500000 width=24)
         Hash Cond: (f.id = d.id)
         ->  Parallel Seq Scan on f  (cost=0.00..109440.79 rows=4998079 width=12)
         ->  Hash  (cost=18109.00..18109.00 rows=1000000 width=12)
               ->  Seq Scan on d  (cost=0.00..18109.00 rows=1000000 width=12)
rows
```

Parallel hash join 自 11 版本开始被支持（enable\_parallel\_hash，默认启用）。通过该特性，build 阶段为每个 Worker 创建 hash table 并在 worker 之间共享，使得 build 阶段更高效。

```sql
testdb=# SET enable_parallel_hash = ON;
SET
testdb=# EXPLAIN SELECT * FROM d, f WHERE d.id = f.id;
                                      QUERY PLAN
--------------------------------------------------------------------------------------
 Gather  (cost=22801.00..304736.59 rows=1000000 width=24)
   Workers Planned: 2
   ->  Parallel Hash Join  (cost=21801.00..203736.59 rows=500000 width=24)
         Hash Cond: (f.id = d.id)
         ->  Parallel Seq Scan on f  (cost=0.00..109440.79 rows=4998079 width=12)
         ->  Parallel Hash  (cost=13109.00..13109.00 rows=500000 width=12)
               ->  Parallel Seq Scan on d  (cost=0.00..13109.00 rows=500000 width=12)
rows
```

## 3.7.3. Parallel Aggregate

PostgreSQL 中几乎所有的聚合函数也都可以并行处理。某个聚合函数是否可以并行化，取决于其在 `Official Document` 中 `Partial Mode` 是否被设置为 `YES`。

当预测的目标行数较少时：

1. 每个 Worker 扫描的行经过 parallel (seq) scan 节点处理后，在 gather 节点处被汇集。
2. 这些被汇集起来的行随后在 aggregate 节点进行聚合。

请看以下示例：

```sql
testdb=# EXPLAIN SELECT avg(id) FROM d where id BETWEEN 1 AND 10;
                                        QUERY PLAN
------------------------------------------------------------------------------------------
 Aggregate  (cost=16609.10..16609.11 rows=1 width=8)
   ->  Gather  (cost=1000.00..16609.10 rows=1 width=8)
         Workers Planned: 2
         ->  Parallel Seq Scan on d  (cost=0.00..15609.00 rows=1 width=8)
               Filter: ((id >= '1'::double precision) AND (id <= '10'::double precision))
rows

testdb=# EXPLAIN SELECT var_pop(id) FROM d where id BETWEEN 1 AND 10;
                                        QUERY PLAN
------------------------------------------------------------------------------------------
 Aggregate  (cost=16609.10..16609.11 rows=1 width=8)
   ->  Gather  (cost=1000.00..16609.10 rows=1 width=8)
         Workers Planned: 2
         ->  Parallel Seq Scan on d  (cost=0.00..15609.00 rows=1 width=8)
               Filter: ((id >= '1'::double precision) AND (id <= '10'::double precision))
rows
```

当预测的目标行数较大时：

1. 每个 Worker 扫描的行经过 parallel sequential scan 节点处理后，在 aggregate 节点进行部分聚合（partial aggregation）。
2. 这些中间聚合结果在 gather 节点被汇集。
3. 最后在 finalize aggregate 节点做最终聚合。

请看以下示例：

```sql
testdb=# EXPLAIN SELECT avg(id) FROM d where data > 100;
                                     QUERY PLAN
-------------------------------------------------------------------------------------
 Finalize Aggregate  (cost=16485.14..16485.15 rows=1 width=8)
   ->  Gather  (cost=16484.93..16485.14 rows=2 width=32)
         Workers Planned: 2
         ->  Partial Aggregate  (cost=15484.93..15484.94 rows=1 width=32)
               ->  Parallel Seq Scan on d  (cost=0.00..14359.00 rows=450371 width=8)
                     Filter: (data > 100)
rows

testdb=# EXPLAIN  SELECT var_pop(id) FROM d where data > 100;
                                     QUERY PLAN
-------------------------------------------------------------------------------------
 Finalize Aggregate  (cost=16485.14..16485.15 rows=1 width=8)
   ->  Gather  (cost=16484.93..16485.14 rows=2 width=32)
         Workers Planned: 2
         ->  Partial Aggregate  (cost=15484.93..15484.94 rows=1 width=32)
               ->  Parallel Seq Scan on d  (cost=0.00..14359.00 rows=450371 width=8)
                     Filter: (data > 100)
rows
```

多个 sum、average 和 variance 可以使用以下公式进行计算：

$$
S_{n} & = S_{n_{1}} + S_{n_{2}} \\ A_{n} & = \frac{1}{n_{1} + n_{2}} \left(\right. S_{n_{1}} + S_{n_{2}} \left.\right) \\ V_{n} & = \left(\right. V_{n_{1}} + V_{n_{2}} \left.\right) + \frac{n_{1} n_{2}}{n_{1} + n_{2}} \left(\left(\right. \frac{S_{n_{1}}}{n_{1}} - \frac{S_{n_{2}}}{n_{2}} \left.\right)\right)^{2}
$$

finalize aggregate 节点使用上述公式对中间结果进行聚合。

对于三个或更多的值，可以通过每次加入一个值的方式迭代重复该过程。

合并多个 Variance

### 已知：

- 一个数据集：{xi|1≤i≤n=n1+n2} ${xi|1≤i≤n=n1+n2}$ $\{ x_{i} | 1 \le i \le n = n_{1} + n_{2} \}$
- 总体平均值：An=∑n1+n2i=1xi $An=∑i=1n1+n2xi$ $A_{n} = \sum_{i=1}^{n_{1}+n_{2}} x_{i}$
- 数据集第一部分的和：Sn1=∑n1i=1xi $Sn1=∑i=1n1xi$ $S_{n_{1}} = \sum_{i=1}^{n_{1}} x_{i}$
- 第一部分的方差：Vn1=∑n1i=1(xi−Sn1n1)2 $Vn1=∑i=1n1(xi−Sn1n1)2$ $V_{n_{1}} = \sum_{i=1}^{n_{1}} (x_{i} - \frac{S_{n_{1}}}{n_{1}})^{2}$
- 数据集第二部分的和：Sn2=∑n1+n2i=1+n1xi $Sn2=∑i=1+n1n1+n2xi$ $S_{n_{2}} = \sum_{i=1+n_{1}}^{n_{1}+n_{2}} x_{i}$
- 第二部分的方差：Vn2=∑n1+n2i=1+n1(xi−Sn2n2)2 $Vn2=∑i=1+n1n1+n2(xi−Sn2n2)2$ $V_{n_{2}} = \sum_{i=1+n_{1}}^{n_{1}+n_{2}} (x_{i} - \frac{S_{n_{2}}}{n_{2}})^{2}$

### 待证：

整个数据集的方差 Vn $Vn$ $V_{n}$ 可以用 Vn1 $Vn1$ $V_{n_{1}}$、Vn2 $Vn2$ $V_{n_{2}}$、Sn1 $Sn1$ $S_{n_{1}}$ 和 Sn2 $Sn2$ $S_{n_{2}}$ 表示如下：

$$
V_{n} = V_{n_{1}} + V_{n_{2}} + \frac{n_{1} n_{2}}{n_{1} + n_{2}} \left(\left(\right. \frac{S_{n_{1}}}{n_{1}} - \frac{S_{n_{2}}}{n_{2}} \left.\right)\right)^{2}
$$

### 证明：

$$
V_{n} & = \sum_{i = 1}^{n} \left(\right. x_{i} - A_{n} \left.\right)^{2} = \sum_{i = 1}^{n_{1}} \left(\right. x_{i} - A_{n} \left.\right)^{2} + \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} \left(\right. x_{i} - A_{n} \left.\right)^{2} \\ = \sum_{i = 1}^{n_{1}} \left(\left(\right. x_{i} - \left(\right. \frac{S_{n_{1}} + S_{n_{2}}}{n_{1} + n_{2}} \left.\right) \left.\right)\right)^{2} + \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} \left(\left(\right. x_{i} - \left(\right. \frac{S_{n_{1}} + S_{n_{2}}}{n_{1} + n_{2}} \left.\right) \left.\right)\right)^{2} \\ = \sum_{i = 1}^{n_{1}} \left(\left(\right. \left(\right. x_{i} - \frac{S_{n_{1}}}{n_{1}} \left.\right) + \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right)\right)^{2} + \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} \left(\left(\right. \left(\right. x_{i} - \frac{S_{n_{2}}}{n_{2}} \left.\right) + \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right)\right)^{2} \\ = \sum_{i = 1}^{n_{1}} \left(\left(\right. x_{i} - \frac{S_{n_{1}}}{n_{1}} \left.\right)\right)^{2} + 2 \left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right) \sum_{i = 1}^{n_{1}} \left(\right. x_{i} - \frac{S_{n_{1}}}{n_{1}} \left.\right) + \sum_{i = 1}^{n_{1}} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right)\right)^{2} \\ + \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} \left(\left(\right. x_{i} - \frac{S_{n_{2}}}{n_{2}} \left.\right)\right)^{2} + 2 \left(\right. \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right) \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} \left(\right. x_{i} - \frac{S_{n_{2}}}{n_{2}} \left.\right) + \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} \left(\left(\right. \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right)\right)^{2} \\ = V_{n_{1}} + 2 \left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right) \left(\right. \sum_{i = 1}^{n_{1}} x_{i} - n_{1} \frac{S_{n_{1}}}{n_{1}} \left.\right) + n_{1} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right)\right)^{2} \\ + V_{n_{2}} + 2 \left(\right. \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right) \left(\right. \sum_{i = 1 + n_{1}}^{n_{1} + n_{2}} x_{i} - n_{2} \frac{S_{n_{2}}}{n_{2}} \left.\right) + n_{2} \left(\left(\right. \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right)\right)^{2} \\ = V_{n_{1}} + 2 \left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right) \left(\right. S_{n_{1}} - S_{n_{1}} \left.\right) + n_{1} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right)\right)^{2} \\ + V_{n_{2}} + 2 \left(\right. \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right) \left(\right. S_{n_{2}} - S_{n_{2}} \left.\right) + n_{2} \left(\left(\right. \frac{\left(\right. - 1 \left.\right) \left(\right. S_{n_{1}} n_{2} - S_{n_{2}} n_{1} \left.\right)}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right)\right)^{2} \\ = V_{n_{1}} + 2 \left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right) \cdot 0 + n_{1} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{1}} \left.\right)\right)^{2} \\ + V_{n_{2}} + 2 \left(\right. \frac{S_{n_{2}} n_{1} - S_{n_{1}} n_{2}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right) \cdot 0 + n_{2} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{\left(\right. n_{1} + n_{2} \left.\right) n_{2}} \left.\right)\right)^{2} \\ = V_{n_{1}} + V_{n_{2}} + \frac{1}{n_{1}} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{n_{1} + n_{2}} \left.\right)\right)^{2} + \frac{1}{n_{2}} \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{n_{1} + n_{2}} \left.\right)\right)^{2} \\ = V_{n_{1}} + V_{n_{2}} + \left(\right. \frac{1}{n_{1}} + \frac{1}{n_{2}} \left.\right) \left(\left(\right. \frac{S_{n_{1}} n_{2} - S_{n_{2}} n_{1}}{n_{1} + n_{2}} \left.\right)\right)^{2} \\ = V_{n_{1}} + V_{n_{2}} + \left(\right. \frac{1}{n_{1}} + \frac{1}{n_{2}} \left.\right) \left(\left(\right. \frac{1}{n_{1} + n_{2}} \left.\right)\right)^{2} \left(\right. S_{n_{1}} n_{2} - S_{n_{2}} n_{1} \left.\right)^{2} \\ = V_{n_{1}} + V_{n_{2}} + \left(\right. \frac{n_{1} + n_{2}}{n_{1} n_{2}} \left.\right) \left(\left(\right. \frac{1}{n_{1} + n_{2}} \left.\right)\right)^{2} \left(\left(\right. \frac{n_{1} n_{2}}{n_{1} n_{2}} \left(\right. S_{n_{1}} n_{2} - S_{n_{2}} n_{1} \left.\right) \left.\right)\right)^{2} \\ = V_{n_{1}} + V_{n_{2}} + \left(\right. \frac{n_{1} + n_{2}}{n_{1} n_{2}} \left.\right) \left(\left(\right. \frac{n_{1} n_{2}}{n_{1} + n_{2}} \left.\right)\right)^{2} \left(\left(\right. \frac{1}{n_{1} n_{2}} \left(\right. S_{n_{1}} n_{2} - S_{n_{2}} n_{1} \left.\right) \left.\right)\right)^{2} \\ = V_{n_{1}} + V_{n_{2}} + \frac{n_{1} n_{2}}{n_{1} + n_{2}} \left(\left(\right. \frac{S_{n_{1}}}{n_{1}} - \frac{S_{n_{2}}}{n_{2}} \left.\right)\right)^{2}
$$
